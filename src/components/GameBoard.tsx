import React from 'react';

const GameBoard: React.FC = () => {
  return (
    <div className="game-board">
      {/* GameBoard component content will go here */}
    </div>
  );
};

export default GameBoard;